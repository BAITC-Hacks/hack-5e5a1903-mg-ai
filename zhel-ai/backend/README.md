# Backend

FastAPI + SQLAlchemy 2.0 (async) + Alembic + PostgreSQL.

Запускается не отсюда, а из корня репозитория одной командой: `docker compose up -d --build`.

Как устроен этот сервис, как добавить свой модуль и какие в нем есть нерешенные места: [../docs/backend.md](../docs/backend.md).
