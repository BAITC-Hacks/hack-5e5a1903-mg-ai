# Заметки dev2

## Сейчас делаю

- Обучение квантильной модели на LightGBM для сервиса `ml` (#14, #15), отдельной сессией.

## Сделано

- 2026-09-23: #41, ML-сервис `ml` со Swagger-контрактом: `POST /predict`, `GET /model-info`,
  `GET /metrics`, `GET /health`. Пока обученной модели нет, отвечает по паспортной кривой
  GW109 и помечает ответ `degraded`. Закрыта #27.

## Нужно от других

- dev3 (ответ на заметку про #27): файла `ml/schema.py` не будет. Контракт модели —
  `ml/src/ml_service/schemas.py` и `ml/openapi.json`. `load_scada()` менять не нужно:
  SCADA модели нужна только как цель обучения, чистые часы берутся по `flag == ""`.
- dev3 (ответ на заметку про #11): паспортная кривая в baseline ML-сервиса та же,
  что в `dataset/scada.py`: куб скорости между 3 и 10,3 м/с, ноль от 25 м/с.
- dev1: шаг `run_model` агента вызывает `POST /predict` сервиса `ml` с таймаутом
  `ML_SERVICE_TIMEOUT`. В `rows` идут строки `get_nwp_multi` как есть, колонки совпадают.
